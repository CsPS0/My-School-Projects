<Window x:Class="solticsongor_Eletjatek.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Életjáték álláskészítő" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Mátrix mérete [sor x oszlop]:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="cbRows" Width="50" Margin="0,0,5,0"/>
            <ComboBox x:Name="cbCols" Width="50"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnCreate" Content="Üres mátrix létrehozása" Padding="5,2" Margin="0,0,10,0" Click="BtnCreate_Click"/>
            <Button x:Name="btnSave" Content="Állás mentése" Padding="5,2" Click="BtnSave_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <UniformGrid x:Name="matrixGrid" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        </ScrollViewer>
    </Grid>
</Window>
